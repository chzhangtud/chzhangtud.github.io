# Notion 时间投入记录模板

## 导入方式

1. 在 Notion 中选择 `Import` → `CSV`，导入 `notion-time-tracking-template.csv`。
2. 将数据库命名为「时间投入记录」。
3. 确认属性类型：
   - 事项：Title
   - 日期：Date
   - 分类：Select
   - 时长（分钟）：Number
   - 备注：Text
4. 给「分类」添加选项：学习、开发、调研、阅读、运动、休息、其他；之后可以继续自定义。

## 推荐视图

- 今日记录：日期筛选为 Today，按日期降序排列。
- 本周统计：日期筛选为 This week，按「分类」分组，并将「时长（分钟）」的计算方式设为 Sum。
- 月度统计：日期筛选为 This month，按「分类」分组，并将「时长（分钟）」的计算方式设为 Sum。
- 日历：使用「日期」作为日历日期。

## 使用方式

每段时间新增一行，手动填写分钟数即可。例如：

- 阅读论文｜学习｜90 分钟
- 修复博客样式｜开发｜120 分钟
- 查找资料｜调研｜45 分钟

可选地添加一个 Formula 属性「时长（小时）」，公式为：

```notion
round(prop("时长（分钟）") / 60 * 10) / 10
```
